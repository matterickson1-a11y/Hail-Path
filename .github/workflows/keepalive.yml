name: HAIL Path Keepalive

on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake HAIL Path
        run: |
          curl -L --max-time 60 https://hail-path-fpuu5dsajdbcvyufcqxrw9.streamlit.app/ || true